apply from: "$rootDir/standard-java.gradle"

dependencies {
    compile project(':common')
    compile project(':appengine-common')

    compile junit
    compile guava_testlib
    compile hamcrest_all
    compile mockito_core
    compile google_appengine_remote_api
}
